(EXP-11: a text used directly in arithmetic is a compile error, not a runtime
 coercion - LANGUAGE.md:1922 and the restatement at :2025.
 The diagnostic names the variable and the fix, so a leaf emitting this form
 would fail the campaign at compile time rather than at run time.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox EXP-11.vox -o p && ./p
 expected output:
   error: Cannot use text label in arithmetic; cast it first with 'as a number' or 'as a float'.
   no binary produced; compile error
)
a text called label is "12".
a number called total is label add 1.
Print total.
