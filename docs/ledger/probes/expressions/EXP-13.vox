(EXP-13: a list used directly in arithmetic is a compile error. Unlike text and
 buffers the diagnostic offers no cast, because LANGUAGE.md:2161 lists lists
 among the things to cast but no list-to-number cast exists.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox EXP-13.vox -o p && ./p
 expected output:
   error: Cannot use list items in arithmetic.
   no binary produced; compile error
)
a list called items is [1, 2, 3].
a number called total is items add 1.
Print total.
