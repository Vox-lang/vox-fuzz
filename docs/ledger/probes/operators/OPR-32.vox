(OPR-32: the shift count may be a plain integer literal
 (LANGUAGE.md:4899-4900), and may also be a variable - the manual only
 shows the literal form, so the variable form is probed alongside it.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox OPR-32.vox -o p && ./p
 expected output:
   960
   1920
)
a number called lhs is 0b11110000.
a number called places is 3.
Print lhs bit-shift-left 2.
Print lhs bit-shift-left places.
