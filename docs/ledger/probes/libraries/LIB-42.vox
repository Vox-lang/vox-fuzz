(LIB-42 -- the consuming-a-library worked example, LANGUAGE.md:5280-5283,
 reproduced verbatim against the checked-in fixtures/libmathkit.lib/.so
 pair. `see` resolves the .lib, selects the matching name+version block,
 verifies the .so exports every promised symbol, registers the
 signature, and the call type-checks and links normally.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/libraries/LIB-42.vox -o p && ./p
 expected output:
   7
)
see mathkit version "1.0" from "fixtures/libmathkit.lib".

a number called sum is 'add two numbers' of 3 and 4.
Print sum.
