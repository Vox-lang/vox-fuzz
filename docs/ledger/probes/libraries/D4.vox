(D4 -- RESOLVED (vox #62), re-recorded 2026-08-22 against vox 0.4.9.
 LANGUAGE.md:5268-5270: "No returning clause means the function returns
 nothing", and step 5 of consuming a library (LANGUAGE.md:5303) says
 calls "type-check like any other function". Here `greet` genuinely
 returns nothing -- its .lib entry is bare `To greet.`, no returning
 clause -- and used to be silently accepted into a typed variable. Now
 it is a compile error.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/libraries/D4.vox -o p
 expected output:
   error: 'greet' has no declared return type in its .lib entry, so its result cannot be used as a value here
   (compile error - no binary is produced)
)
see mathkit version "1.0" from "fixtures/libmathkit.lib".

a number called n is greet.
Print n.
