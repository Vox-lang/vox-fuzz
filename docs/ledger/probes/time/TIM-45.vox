(TIM-45 - LANGUAGE.md:4645-4646 marks `duration` and `elapsed` "requires
 cast", but both read fine with no cast at all, and the uncast reading is
 the seconds one. See Discrepancy 2.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox TIM-45.vox -o p && ./p
 expected output:
   the uncast duration is the seconds reading
   the uncast elapsed is the seconds reading too
)
a timer called 'job timer'.
Start the 'job timer'.
Wait 1500 milliseconds.
Stop the 'job timer'.
a number called 'the uncast duration' is the 'job timer''s duration.
a number called 'the cast duration' is the 'job timer''s duration in seconds.
a number called 'the uncast elapsed' is the 'job timer''s elapsed.
If 'the uncast duration' is 'the cast duration' then, print "the uncast duration is the seconds reading".
Otherwise, print "uncast {'the uncast duration'} against seconds {'the cast duration'}".
If 'the uncast elapsed' is 'the cast duration' then, print "the uncast elapsed is the seconds reading too".
Otherwise, print "uncast elapsed {'the uncast elapsed'} against seconds {'the cast duration'}".
