(Discrepancy 2 — RESOLVED (vox #44), re-recorded 2026-08-22 against vox
 0.4.9. Used to be: a LIST in a format string used as a VALUE rendered
 the list's raw pointer instead of the list. LANGUAGE.md:3368 says a
 format string used as a value "materializes into a fresh NUL-terminated
 string"; 3158-3161 says one shared name resolver makes every sink render
 identically. On 0.4.8 the same slot in a Print rendered correctly but
 the value-context sink did not; now both agree.
 Lines 1 and 2 below are the list printed directly and through a print
 slot: both correct, unchanged. Line 3 is the same slot held in a text
 first, checked as a comparison rather than a raw print because on 0.4.8
 the address varied per run; now that it's fixed this is moot but the
 comparison form still re-runs clean either way.
 Second facet, re-verified separately (not part of this probe's fixed
 block): `Print "{arguments's all}".` now prints `[]`, matching
 `Print arguments's all.` exactly - it used to print a raw pointer.
 The buffer sink for both facets was unblocked by #52 (Discrepancy 1)
 and not separately re-probed here; see D1.vox.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox probe.vox -o p && ./p
 expected output:
   [1, 2, 3]
   [1, 2, 3]
   a list in a format string held as a value is the list form
)
a list called ordinary is [1, 2, 3].
a text called 'the held list' is "{ordinary}".
Print ordinary.
Print "{ordinary}".
If 'the held list' is equal to "[1, 2, 3]" then, Print "a list in a format string held as a value is the list form". Otherwise, Print "a list in a format string held as a value is NOT the list form".
