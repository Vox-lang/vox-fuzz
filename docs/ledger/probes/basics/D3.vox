(D3 - Discrepancy 3 repro: an unterminated comment silently swallows the
 rest of the file. LANGUAGE.md:53-74 documents multi-line comments and
 says nothing about an unbalanced open-paren; the compiler emits no error
 and no warning, produces a binary, and exits 0 having run only the
 statements before the stray open-paren. Contrast LANGUAGE.md:88, where
 the comparable "construct still open at end of file" case for a function
 definition DOES get a warning.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox D3.vox -o p && ./p
 expected output:
   a
)
print "a".
(unterminated comment starts here
print "b".
print "c".
