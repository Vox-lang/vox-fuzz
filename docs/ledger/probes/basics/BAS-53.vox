(BAS-53 - TWO periods: the inner if is closed, so the Otherwise belongs to
 the OUTER one. LANGUAGE.md:231-240, verbatim. Prints `outer else` then
 `done` - one character different from BAS-52.
 `But if` behaves the same way in this position (verified separately:
 replacing `Otherwise,` with `But if m is equal to 5 then,` prints
 `outer but if` then `done`).
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox BAS-53.vox -o p && ./p
 expected output:
   outer else
   done
)
a number called m is 5.
If m is equal to 1 then,
    If m is equal to 2 then,
        print "inner then"..
    Otherwise,
        print "outer else".

Print "done".
