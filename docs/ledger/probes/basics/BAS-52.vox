(BAS-52 - ONE period: the Otherwise belongs to the INNER if.
 LANGUAGE.md:214-223, verbatim. Prints only `done`.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox BAS-52.vox -o p && ./p
 expected output:
   done
)
a number called m is 5.
If m is equal to 1 then,
    If m is equal to 2 then,
        print "inner then".
    Otherwise,
        print "outer else".

Print "done".
