(KEY-86 — every word in the new Reserved Nouns and Properties table
 (LANGUAGE.md:5258-5272, 0.4.15, #106 second half / GitHub #239) is
 reserved as a variable name. Resolves the rest of Discrepancy 4 (see
 below). Hand-verified across eight of the ten words:

   arguments, environment, standard, byte, elapsed, error, exists, variable

 All eight hand-run outputs:
   error: Cannot use 'arguments' as a variable name - it's a reserved keyword.
   error: Cannot use 'environment' as a variable name - it's a reserved keyword.
   error: Cannot use 'standard' as a variable name - it's a reserved keyword.
   error: Cannot use 'byte' as a variable name - it's a reserved keyword.
   error: Cannot use 'elapsed' as a variable name - it's a reserved keyword.
   error: Cannot use 'error' as a variable name - it's a reserved keyword.
   error: Cannot use 'exists' as a variable name - it's a reserved keyword.
   error: Cannot use 'variable' as a variable name - it's a reserved keyword.

 Note: `byte` is reserved here as a bare declaration; its one narrow
 carve-out as the `each ... from <buffer>` loop-variable name (BAS2-61,
 LANGUAGE.md:352-353) is a different, single grammatical position, not
 a contradiction of this row.

 `docs/check-probes.sh` mechanically re-runs and matches only the first
 (`arguments`, below); the rest are the retained record per
 PROCEDURE.md §4.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/worktrees/wt-stack-0415/coreasm /home/josj/scr/english/vox-notes/stack-0415/vox-stack KEY-86.vox -o p && ./p
 expected output:
   error: Cannot use 'arguments' as a variable name - it's a reserved keyword.
   (no binary produced; compile error)
)
a number called arguments is 1.
print arguments.
