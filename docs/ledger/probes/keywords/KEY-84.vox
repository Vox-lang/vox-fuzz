(KEY-84 — every word in the new Types table (LANGUAGE.md:5132-5150,
 0.4.15, #106 second half / GitHub #239) is reserved as a variable
 name. Hand-verified across eight of the thirteen words:

   float, boolean, list, map, timer, nothing, int, number

 All eight hand-run outputs:
   error: Cannot use 'float' as a variable name - it's a reserved keyword.
   error: Cannot use 'boolean' as a variable name - it's a reserved keyword.
   error: Cannot use 'list' as a variable name - it's a reserved keyword.
   error: Cannot use 'map' as a variable name - it's a reserved keyword.
   error: Cannot use 'timer' as a variable name - it's a reserved keyword.
   error: Cannot use 'nothing' as a variable name - it's a reserved keyword.
   error: Cannot use 'int' as a variable name - it's a reserved keyword.
   error: Cannot use 'number' as a variable name - it's a reserved keyword.

 `docs/check-probes.sh` mechanically re-runs and matches only the first
 (`float`, below); the rest are the retained record per PROCEDURE.md §4.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/worktrees/wt-stack-0415/coreasm /home/josj/scr/english/vox-notes/stack-0415/vox-stack KEY-84.vox -o p && ./p
 expected output:
   error: Cannot use 'float' as a variable name - it's a reserved keyword.
   (no binary produced; compile error)
)
a number called float is 1.
print float.
