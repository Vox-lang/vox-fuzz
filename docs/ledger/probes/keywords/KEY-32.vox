(KEY-32 — `with` introduces function parameters in a definition and
 arguments at a call site.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox KEY-32.vox -o p && ./p
 expected output:
   8
)
To 'sum of' with a number called left and a number called right.
    Return a number, left add right.

Print 'sum of' with 3 and 5.
