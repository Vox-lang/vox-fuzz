(KEY-85 — every non-hyphenated word in the new Operators table
 (LANGUAGE.md:5153-5162, 0.4.15, #106 second half / GitHub #239) is
 reserved as a variable name. The six bitwise spellings (`bit-and`,
 `bit-or`, `bit-xor`, `bit-not`, `bit-shift-left`, `bit-shift-right`)
 are hyphenated multi-part tokens, not bare identifiers (LANGUAGE.md
 :5015, "lhs bit-and rhs"), so they cannot be tested by "declare a
 variable with this spelling" the way the rest of the table can; they
 are excluded from the sample for that structural reason. Hand-verified
 across eight of the remaining words:

   add, greater, not, even, empty, zero, equals, than

 All eight hand-run outputs:
   error: Cannot use 'add' as a variable name - it's a reserved keyword.
   error: Cannot use 'greater' as a variable name - it's a reserved keyword.
   error: Cannot use 'not' as a variable name - it's a reserved keyword.
   error: Cannot use 'even' as a variable name - it's a reserved keyword.
   error: Cannot use 'empty' as a variable name - it's a reserved keyword.
   error: Cannot use 'zero' as a variable name - it's a reserved keyword.
   error: Cannot use 'equals' as a variable name - it's a reserved keyword.
   error: Cannot use 'than' as a variable name - it's a reserved keyword.

 `docs/check-probes.sh` mechanically re-runs and matches only the first
 (`add`, below); the rest are the retained record per PROCEDURE.md §4.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/worktrees/wt-stack-0415/coreasm /home/josj/scr/english/vox-notes/stack-0415/vox-stack KEY-85.vox -o p && ./p
 expected output:
   error: Cannot use 'add' as a variable name - it's a reserved keyword.
   (no binary produced; compile error)
)
a number called add is 1.
print add.
