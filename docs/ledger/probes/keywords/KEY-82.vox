(KEY-82 — every word in the Statement Starters table (LANGUAGE.md:5044-
 5082, 0.4.15, #106 second half / GitHub #239) is reserved as a variable
 name. Hand-verified across eight of the fourteen new words, spread
 across the table, each compiled and run separately since a compile
 error halts the program at the first offending declaration:

   read, write, open, close, wait, sleep, get, clear, copy, resize,
   seek, repeat, see, library

 All eight hand-run outputs, each its own `vox <file> -o p` invocation
 (`a number called <word> is 1. print <word>.`):
   error: Cannot use 'read' as a variable name - it's a reserved keyword.
   error: Cannot use 'close' as a variable name - it's a reserved keyword.
   error: Cannot use 'sleep' as a variable name - it's a reserved keyword.
   error: Cannot use 'clear' as a variable name - it's a reserved keyword.
   error: Cannot use 'resize' as a variable name - it's a reserved keyword.
   error: Cannot use 'seek' as a variable name - it's a reserved keyword.
   error: Cannot use 'repeat' as a variable name - it's a reserved keyword.
   error: Cannot use 'library' as a variable name - it's a reserved keyword.

 `docs/check-probes.sh` mechanically re-runs and matches only the first
 (`read`, below); the other seven are the retained record per
 PROCEDURE.md §4.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/worktrees/wt-stack-0415/coreasm /home/josj/scr/english/vox-notes/stack-0415/vox-stack KEY-82.vox -o p && ./p
 expected output:
   error: Cannot use 'read' as a variable name - it's a reserved keyword.
   (no binary produced; compile error)
)
a number called read is 1.
print read.
