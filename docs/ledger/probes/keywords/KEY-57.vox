(KEY-57 — the property keyword `empty` is reserved as a variable name.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox KEY-57.vox -o p && ./p
 expected output:
   error: Cannot use 'empty' as a variable name - it's a reserved keyword.
     Tip: Try a more descriptive name like 'empty_value' or 'my_empty'
   (no binary produced; compile error)
)
a number called empty is 1.
Print empty.
