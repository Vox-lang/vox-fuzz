(Discrepancy 8 repro — a missing REQUIRED flag (4651) ends the program with
 exit status 1 and no diagnostic at all: nothing on stdout, nothing on
 stderr. The manual says only that the flag is "required ... at runtime"
 and never says what a violation looks like.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox D8.vox -o p && ./p
 expected output:
   (exit 1 - no output at all)
)
a flag called retries is "-r" or "--retries", it is a number and is required.
Parse flags.
Print retries.
