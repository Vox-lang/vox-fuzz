(KEY-12 — `Break` leaves the enclosing loop immediately.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox KEY-12.vox -o p && ./p
 expected output:
   1
   2
   after the loop
)
For each step from 1 to 5,
    If step is 3 then,
        Break.
    Print step.
Print "after the loop".
