(THG-70: a thing cannot be interpolated into a TEXT INITIALIZER - a text
 initializer is a different sink from `Print`, which does render one.
 LANGUAGE.md:1246-1248. The caret is correct here: line 6, the slot itself.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox THG-70.vox -o p && ./p
 expected output:
   error: 'origin' holds a whole point, which only `Print` can interpolate
       A whole thing renders as its fields (plan 310 §7) and `Print` writes that straight out; building text from one is not written yet.
       Interpolate a field instead - point's fields are: x, y
   (no binary produced; compile error)
)
A thing called point has
  a number called x is 0,
  a number called y is 0.

a point called origin.
a text called label is "at {origin}".
Print label.
