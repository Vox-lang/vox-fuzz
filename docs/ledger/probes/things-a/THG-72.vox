(THG-72: printing a call's result directly needs a variable - the result is
 a whole thing that must land in storage before it can be read.
 Also covers: THG-73 (the exact diagnostic, LANGUAGE.md:1226-1239).
 The caret lands on line 5, the `To nudged` definition, not on line 10, the
 offending `Print`. See Discrepancy 3.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox THG-72.vox -o p && ./p
 expected output:
   error: A call to 'nudged' returns a whole point, which is not a value
       What a call returns is copied into a point: write `a point called <name> is nudged of ...` or `The <name> is nudged of ...` (plan 310 §5).
   (no binary produced; compile error)
)
A thing called point has
  a number called x is 0,
  a number called y is 0.

To nudged with a point called start.
  Set start's x to start's x add 1.
  Return a point, start.

a point called before.
Print nudged of before.
