(BUF-14: universal `type` property should report `Buffer (static)` for a buffer variable, per LANGUAGE.md:3202-3208.)
(expected output:
Text (dynamic)
Buffer (static)
)
a buffer called buf1 is 16 bytes in size.
a buffer called buf2 is "seed".
print buf1's type.
print buf2's type.
