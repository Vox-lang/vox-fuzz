(BUF-14 (updated after vox #42 fixed the property — see buffers.md D2): universal `type` property should report `Buffer (static)` for a buffer variable, per LANGUAGE.md:3346-3350.)
(expected output:
Buffer (static)
Buffer (static)
)
a buffer called buf1 is 16 bytes in size.
a buffer called buf2 is "seed".
print buf1's type.
print buf2's type.
