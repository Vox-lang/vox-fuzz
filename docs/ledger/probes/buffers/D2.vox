(Discrepancy 2: LANGUAGE.md:3202-3208 says a statically-typed variable, buffer included, reports its `type` property with "(static)". A buffer declared with a byte count is mistagged as "Text (dynamic)"; a buffer declared from a string literal is tagged correctly.)
(expected output:
Number (static)
Text (dynamic)
Buffer (static)
)
a number called n is 3.
a buffer called buf1 is 16 bytes in size.
a buffer called buf2 is "seed".
print n's type.
print buf1's type.
print buf2's type.
