(BUF-54: "A list also accepts `Free`, with the same after-state a
 buffer gets ..." (LANGUAGE.md:3699-3708). RESOLVED, vox #109, 0.4.15 —
 see Discrepancy 4. Hand-verified against the 0.4.15 stack: the list is
 empty after `Free` (length 0, empty true, prints []), and the
 following `append` is refused with the error flag.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/worktrees/wt-stack-0415/coreasm /home/josj/scr/english/vox-notes/stack-0415/vox-stack BUF-54.vox -o p && ./p
 expected output:
   0
   1
   []
   append to freed list refused
   0
   []
)
a list called nums is [1, 2, 3].
Free nums.
print nums's length.
print nums's empty.
print nums.
append 9 to nums.
On error print "append to freed list refused".
print nums's length.
print nums.
