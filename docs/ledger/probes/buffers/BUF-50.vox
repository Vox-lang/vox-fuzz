(BUF-50: the worked example at LANGUAGE.md:3637-3643 — declare a buffer
 from a string, `Free` it, print its length (0) and `as text` ("[]"),
 then `append` and catch the refusal with `On error` — compiles and
 behaves exactly as shown.
 Ran: VOX_CORE_PATH=/usr/share/vox/coreasm /usr/bin/vox BUF-50.vox -o p && ./p
 expected output:
   0
   []
   refused: line is freed
)
a buffer called line is "hello".
Free line.
print line's length.
print "[{line as text}]".

append "more" to line.
On error print "refused: line is freed".
