(D8 — RESOLVED (vox #53, same fix as D7), re-recorded 2026-08-22 against
 vox 0.4.9. Used to be: SEGFAULT. Six lines of legal Vox. Two
 string-initialised buffers, plus one buffer received from a function
 whose return expression is a text literal (`Return a buffer, "ABC".`),
 and the program died before printing anything. Deterministic: exit 139,
 3/3 runs, a top-severity memory-safety fault on a program that compiled
 clean. Now the construct is refused at compile time regardless of how
 many buffers exist, so the dose no longer matters — same diagnostic as
 D7.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox D8.vox -o p
 expected output:
   error: Cannot return text "ABC" as a buffer; the caller reads what Return hands back as a buffer, and text is not one. Build the buffer first: 'a buffer called made is "ABC". Return a buffer, made.'
   (compile error - no binary is produced)
)
To 'give literal'. Return a buffer, "ABC".

a buffer called direct is "ABC".
a buffer called second is "DEF".
a buffer called 'from literal' is 'give literal'.
Print 'from literal''s size.
