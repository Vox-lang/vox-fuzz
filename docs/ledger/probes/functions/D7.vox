(D7 — RESOLVED (vox #53), re-recorded 2026-08-22 against vox 0.4.9. Used
 to be: `Return a buffer, "ABC".` — a text literal in the return
 expression of a buffer-returning function — yielded an EMPTY buffer at
 the call site, size 0, printing nothing, where the identical text in a
 buffer DECLARATION worked. Now the construct is refused at compile
 time, naming the exact working rewrite.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox D7.vox -o p
 expected output:
   error: Cannot return text "ABC" as a buffer; the caller reads what Return hands back as a buffer, and text is not one. Build the buffer first: 'a buffer called made is "ABC". Return a buffer, made.'
   (compile error - no binary is produced)
)
To 'give literal'. Return a buffer, "ABC".

a buffer called direct is "ABC".
Print direct's size.
Print direct.
a buffer called 'from literal' is 'give literal'.
Print 'from literal''s size.
Print 'from literal'.
