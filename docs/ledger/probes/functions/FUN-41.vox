(FUN-41 — (gap) a function with NO declared return type, used in
 expression position, yields an untyped machine word: `announce` returns
 nothing and `Print announce of 9.` prints 1. This is vox bug #45
 (vox/docs/BUGS_FOUND.md:2931), already filed from the collections-a
 ledger; recorded here because this section is where the shape is
 defined and a leaf must avoid emitting it until #45 closes.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox FUN-41.vox -o p && ./p
 expected output:
   got 9
   1
)
To announce with a number called n.
  Print "got {n}".

Print announce of 9.
