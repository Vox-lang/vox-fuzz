(THG2-46: a thing is defined at the top level, like a function; a definition
 inside a FUNCTION BODY is a compile error and the message says to move it
 above the block. LANGUAGE.md:1703-1709, verbatim.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox THG2-46.vox -o p && ./p
 expected output:
   error: A thing is defined at the top level, like a function
   followed by "Move the definition above the block it is written in".
   No binary is produced; this is a compile error.
)
To 'take a reading'.
  A thing called measurement has
    a number called degrees is 0.
  Print "taken".
