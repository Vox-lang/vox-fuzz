(VAR-35: reusing an already-declared name as a for-range loop variable does
 reject a conflicting type — the half of LANGUAGE.md:625-627 that works.
 (Compile-error claim: not assertable from a runtime leaf.)
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox VAR-35.vox -o p && ./p
 expected output:
   error: cannot bind 'step' to a number in this for-range loop; 'step' is already declared as text
   note: 'step' was declared as text
   help: use a different name here, or declare 'step' as a number instead
   (no binary produced; compile error)
)
a text called step is "x".
For each step from 1 to 3,
  Print step.
