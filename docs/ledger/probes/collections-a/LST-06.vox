(LST-06 — a list may freely hold numbers, texts, decimals and booleans
 together, and every element reads back as what it is.
 Also covers: LST-09 (each element prints as its own type), LST-10 (the
 worked example of LANGUAGE.md:2343-2345, which prints 1 / two / 3.5 / 1).
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox LST-06.vox -o p && ./p
 expected output:
   1
   two
   3.5
   1
   [1, "two", 3.5, 1]
   4
)
a list called mixedbag is [1, "two", 3.5, yes].
For each item in mixedbag, print item.
print mixedbag.
print mixedbag's length.
