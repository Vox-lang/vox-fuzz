(LST-43 — `Set map's "key" to value` inserts a new entry or replaces an
 existing one.
 Also covers: LST-45 (the worked example of LANGUAGE.md:2443-2445 — a
 replace leaves `length` at 2, it does not insert).
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox LST-43.vox -o p && ./p
 expected output:
   37
   2
   inserted
   3
)
a map called person is {"name": "Ada", "age": 36}.
set person's "age" to 37.
print person's "age".
print person's length.
set person's "city" to "inserted".
print person's "city".
print person's length.
