(LST-19 — RE-RECORDED 2026-08-22 against vox 0.4.9. This used to be the
 worked widening example of old manual lines 2238-2243, repaired to quote
 `"hello"` (the manual's own unquoted `hello` was Discrepancy 1, now
 fixed — the current manual quotes it too, at 2288). What changed since:
 the SECOND half of this same program, `append five of 4 to items.`
 (five has no declared return type), used to compile and print `5`; vox
 #45 now refuses it at compile time (Discrepancy 5). So this exact
 program — which the manual used to show as fully working — is now
 exactly the manual's OWN worked example of the compile error
 (LANGUAGE.md:2372-2375, verbatim).
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox LST-19.vox -o p
 expected output:
   error: 'five' has no declared return type, so its result is read as a number here
   (compile error - no binary is produced)
)
To five with a number called x. Return x add 1.

a list called items is [].
append "hello" to items.
append five of 4 to items.
print element 1 of items.
print element 2 of items.
print items.
