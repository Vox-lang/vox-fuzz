(LST-18 — RE-RECORDED 2026-08-22 against vox 0.4.9. Appending an OPAQUE
 value (a function with no declared return type) to a list USED TO widen
 the list to mixed and print a raw address for a text (Discrepancy 5).
 vox #45 fixed it: this is now a COMPILE ERROR, one per opaque append,
 naming both the "declare the return type" and "assign to a declared
 variable first" rewrites. LANGUAGE.md:2418-2426.
 LST-21 (the withdrawn residual TAG_INTEGER-guess row) described the
 pre-fix hedge this probe used to demonstrate; there is no hedge left to
 demonstrate now that the write is refused outright.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox LST-18.vox -o p
 expected output:
   error: 'opaque count' has no declared return type, so its result is read as a number here
   (compile error - no binary is produced; a second, identical-shaped error follows for 'opaque label')
)
To 'opaque label'. Return "hi".

To 'opaque count' with a number called seed. Return seed add 1.

a list called items is [].
append "anchor" to items.
append 'opaque count' of 4 to items.
append 'opaque label' to items.
print items.
print element 2 of items.
print element 3 of items.
For each item in items,
  if item is a text, print "text". otherwise if item is a number, print "number". otherwise print "other".
