(LST-30 — `is a list` recognises a nested-list element by its runtime tag,
 and folds to true on a statically-typed list variable.
 Also covers: LST-31 (the worked example of LANGUAGE.md:2408-2410, which
 prints s / L / s).
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox LST-30.vox -o p && ./p
 expected output:
   s
   L
   s
   static list folds true
)
For each item in [1, [2, 3], "x"],
  if item is a list then, print "L". otherwise print "s".

a list called shelf is [1, 2].
if shelf is a list then, print "static list folds true". otherwise print "static list folds false".
