(D5 — RESOLVED, re-recorded 2026-08-22 against vox 0.4.9. Old manual
 lines 2232-2235 (pre-0.4.9) said an opaque value "widens the list to
 mixed, so the element is always read back as what it is rather than
 silently reinterpreted". A function with no declared return type that
 returned a TEXT USED TO be silently reinterpreted as a number: the slot
 printed a raw address (stable across runs, 4210906, 5/5) and `is a
 number` fired on it. Old line 2250-2254 conceded a conservative
 TAG_INTEGER guess, so the two paragraphs disagreed with each other.
 vox #45 fixed the COMPILER, not just the docs: the write is now refused
 outright. LANGUAGE.md:2298-2306 states the current (single, consistent)
 rule.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox D5.vox -o p
 expected output:
   error: 'opaque label' has no declared return type, so its result is read as a number here
   (compile error - no binary is produced)
)
To 'opaque label'. Return "hi".

a list called items is [].
append "anchor" to items.
append 'opaque label' to items.
print element 2 of items.
For each item in items,
  if item is a text, print "text". otherwise if item is a number, print "number". otherwise print "other".
