(FIL-68: Close accepts both the bare name and the `the <name>` article form, and a read on a closed handle sets the error flag and returns 0 bytes rather than crashing.
 Also covers: FIL-69, FIL-101.
 Run from the vox-fuzz worktree root; the probe creates everything it
 needs under vf_scratch/files/ and touches nothing else.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/files/FIL-68.vox -o p && ./p
 actual output:
   Close the <name> with the article compiled and ran
   Close <name> without the article compiled and ran
   reading a closed handle set the error flag
   0
)
Create a directory called "vf_scratch".
Create a directory called "vf_scratch/files".
open a file for writing called seedfile at "vf_scratch/files/lines.txt".
Write "alpha\nbeta\ngamma\n" to seedfile.
Close seedfile.
open a file for reading called source at "vf_scratch/files/lines.txt".
Close the source.
print "Close the <name> with the article compiled and ran".
open a file for reading called second at "vf_scratch/files/lines.txt".
Close second.
print "Close <name> without the article compiled and ran".
a buffer called chunk is 16 bytes in size.
Read from second into chunk.
On error print "reading a closed handle set the error flag".
print chunk's size.
