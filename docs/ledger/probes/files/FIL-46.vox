(FIL-46: Read line from ... into ... resets the buffer before each read, so pre-existing content is discarded (replace, not append).
 Also covers: FIL-55.
 Run from the vox-fuzz worktree root; the probe creates everything it
 needs under vf_scratch/files/ and touches nothing else.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/files/FIL-46.vox -o p && ./p
 actual output:
   5
   6
   alpha
  
)
Create a directory called "vf_scratch".
Create a directory called "vf_scratch/files".
open a file for writing called seedfile at "vf_scratch/files/lines.txt".
Write "alpha\nbeta\ngamma\n" to seedfile.
Close seedfile.
open a file for reading called source at "vf_scratch/files/lines.txt".
a buffer called linebuf is 64 bytes in size.
append "STALE" to linebuf.
print linebuf's size.
Read line from source into linebuf.
print linebuf's size.
print linebuf.
Close source.
