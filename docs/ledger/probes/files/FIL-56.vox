(FIL-56: for a fixed-size buffer an overlong line is truncated to capacity and the error flag is set.
 Run from the vox-fuzz worktree root; the probe creates everything it
 needs under vf_scratch/files/ and touches nothing else.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/files/FIL-56.vox -o p && ./p
 actual output:
   the overlong line set the error flag
   2
   on
)
Create a directory called "vf_scratch".
Create a directory called "vf_scratch/files".
open a file for writing called seedfile at "vf_scratch/files/noeol.txt".
Write "one\ntail-without-newline" to seedfile.
Close seedfile.
open a file for reading called source at "vf_scratch/files/noeol.txt".
a buffer called tiny is 2 bytes in size.
Read line from source into tiny.
On error print "the overlong line set the error flag".
print tiny's size.
print tiny.
Close source.
