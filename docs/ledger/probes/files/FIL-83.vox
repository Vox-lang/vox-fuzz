(FIL-83: the documented manual truncation check - comparing a buffer's size against its capacity after a read - compiles and fires when a fixed buffer filled up.
 Run from the vox-fuzz worktree root; the probe creates everything it
 needs under vf_scratch/files/ and touches nothing else.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/files/FIL-83.vox -o p && ./p
 actual output:
   Warning: buffer may have been truncated
   8
   8
)
Create a directory called "vf_scratch".
Create a directory called "vf_scratch/files".
open a file for writing called seedfile at "vf_scratch/files/lines.txt".
Write "alpha\nbeta\ngamma\n" to seedfile.
Close seedfile.
open a file for reading called source at "vf_scratch/files/lines.txt".
a buffer called sink is 8 bytes in size.
Read from source into sink.
If sink's size is equal to sink's capacity then,
    print "Warning: buffer may have been truncated".
print sink's size.
print sink's capacity.
Close source.
