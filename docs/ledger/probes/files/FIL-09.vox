(FIL-09: the File Properties worked example (open for reading, print size, print modified, compare size against 1048576) compiles and behaves as the surrounding prose says.
 Run from the vox-fuzz worktree root; the probe creates everything it
 needs under vf_scratch/files/ and touches nothing else.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/files/FIL-09.vox -o p && ./p
 actual output:
   16
   modified is a plausible unix timestamp
   the size comparison did not fire
)
Create a directory called "vf_scratch".
Create a directory called "vf_scratch/files".
open a file for writing called seedfile at "vf_scratch/files/data.txt".
Write "sixteen bytes ok" to seedfile.
Close seedfile.
open a file for reading called src at "vf_scratch/files/data.txt".
print src's size.
a number called 'the modification time' is src's modified.
If 'the modification time' is greater than 1700000000 then,
    print "modified is a plausible unix timestamp".
If src's size is greater than 1048576 then,
    print "File is larger than 1MB".
print "the size comparison did not fire".
Close src.
