(FIL-102: line N exists when the file holds at least N-1 newlines before
 it — a file ending in a newline has one empty last line to seek to;
 anything beyond that is past EOF and errors. LANGUAGE.md:4016-4018, the
 new bullet the #47 fix (Discrepancy 3) brought with it.
 File "AA\nBB\n" has 2 newlines, so line 3 is the one empty trailing
 line (exists, size 0, no error) and line 4 is past it (errors).
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/files/FIL-102.vox -o p && ./p
 expected output:
   0
   line 4 errored
)
Create a directory called "vf_scratch".
Create a directory called "vf_scratch/files".
open a file for writing called seedfile at "vf_scratch/files/fil102.txt".
Write "AA\nBB\n" to seedfile.
Close seedfile.
open a file for reading called src at "vf_scratch/files/fil102.txt".
a buffer called lastline is 32 bytes in size.
Seek src to line 3.
Read line from src into lastline.
On error print "line 3 errored".
print lastline's size.
Seek src to line 4.
Read line from src into lastline.
On error print "line 4 errored".
