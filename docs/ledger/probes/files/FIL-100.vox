(FIL-100: closing an already-closed handle neither crashes nor sets the error flag - the double-free-is-impossible claim, from the file-handle side.
 Also covers: FIL-86.
 Run from the vox-fuzz worktree root; the probe creates everything it
 needs under vf_scratch/files/ and touches nothing else.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/files/FIL-100.vox -o p && ./p
 actual output:
   a double Close neither crashed nor raised
)
Create a directory called "vf_scratch".
Create a directory called "vf_scratch/files".
open a file for writing called seedfile at "vf_scratch/files/lines.txt".
Write "alpha\nbeta\ngamma\n" to seedfile.
Close seedfile.
open a file for reading called source at "vf_scratch/files/lines.txt".
Close source.
Close source.
On error print "the second Close set the error flag".
print "a double Close neither crashed nor raised".
