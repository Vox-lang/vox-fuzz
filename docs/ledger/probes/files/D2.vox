(D2: Discrepancy 2 - `Read from ... into ...` REPLACES the destination buffer's contents; LANGUAGE.md:3752 says it appends.
 Both halves shown: a fixed 128-byte buffer with 4 bytes in it and room for
 all 17 more, and a dynamic buffer with the same 4 bytes. Both report 17,
 not 21. The printed content is the file's, with no "PRE-" prefix.
 Run from the vox-fuzz worktree root; the probe creates everything it
 needs under vf_scratch/files/ and touches nothing else.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox docs/ledger/probes/files/D2.vox -o p && ./p
 actual output:
   4
   17
   alpha
   beta
   gamma
  
   4
   17
)
Create a directory called "vf_scratch".
Create a directory called "vf_scratch/files".
open a file for writing called seedfile at "vf_scratch/files/lines.txt".
Write "alpha\nbeta\ngamma\n" to seedfile.
Close seedfile.
(A 128-byte fixed buffer holding 4 bytes, with room for all 17 more.)
a buffer called sink is 128 bytes in size.
append "PRE-" to sink.
print sink's size.
open a file for reading called source at "vf_scratch/files/lines.txt".
Read from source into sink.
print sink's size.
print sink.
Close source.
(The same again on a dynamic buffer, in case the fixed form is special.)
a buffer called 'the growing sink' is "PRE-".
print 'the growing sink's size.
open a file for reading called 'the second source' at "vf_scratch/files/lines.txt".
Read from 'the second source' into 'the growing sink'.
print 'the growing sink's size.
Close 'the second source'.
