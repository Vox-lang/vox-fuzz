(VAL-13: a failed conversion sets `_last_error` and leaves the variable as
 0 — a text that cannot parse as a number yields 0 and raises the flag so
 `On error` catches it.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox VAL-13.vox -o p && ./p
 expected output:
   cast failed
   0
)
a value called bad is "abc".
bad is a number.
on error print "cast failed".
print bad.
