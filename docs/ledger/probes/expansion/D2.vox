(D2 — Discrepancy 2 repro: LANGUAGE.md:424 says "If no conditions match, the
 default action runs", and :389 says the default action is the base
 statement. With a trailing `otherwise` present, no condition matches on 1
 and 3, and the base statement `append <the number> to out` does NOT run —
 the otherwise runs instead. The list is [99, 0, 99], not [1, 0, 3].
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox D2.vox -o p && ./p
 expected output:
   [99, 0, 99]
)
a list called out is [].
append each number from 1 to 3 to out,
    but if the number modulo 2 is equal to 0 append 0,
    otherwise append 99.
print out.
