(D1 — RESOLVED for this arm, re-recorded 2026-08-22 against vox 0.4.9.
 Used to be: loop expansion did NOT work with "any action that takes an
 argument" (LANGUAGE.md:312, and "works with any action" at :284) for
 this arm specifically. `append each word from [...] to sink` (sink a
 buffer) now compiles and works, a side effect of vox #54/#55. The other
 two refuted arms (`write each word from [...] to output.`, `Set total
 to each number from 1 to 3.`) are unchanged — still refused identically,
 re-verified separately.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox D1.vox -o p && ./p
 expected output:
   abcd
)
a buffer called sink is 64 bytes in size.
append each word from ["ab", "cd"] to sink.
print sink.
