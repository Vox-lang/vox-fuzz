(D3 — RESOLVED (vox #55), re-recorded 2026-08-22 against vox 0.4.9. Used
 to be: a `treating` clause whose match and replacement are numbers, over
 a collection whose elements are text, compiled without a diagnostic and
 segfaulted (deterministic, 6/6 runs, exit 139). Now it is a compile
 error naming the mismatch.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox D3.vox -o p
 expected output:
   error: Treating value and match must be the same type (got text vs number).
   (no binary produced; compile error)
)
print each item from ["a"] treating 98 as 31.
