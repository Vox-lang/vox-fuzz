(FLW-17: inside the loop, `the number` is the current iteration value -
 and the bare `number` (no article) is the same value, so both spellings
 print the same thing on the same iteration. The running total proves it
 is the ITERATION value and not a constant: 3+4+5+6 is 18.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox FLW-17.vox -o p && ./p
 expected output:
   18
   2
   2
   3
   3
   4
   4
)
a number called total is 0.
For each number from 3 to 6, Set total to total add number.
print total.
For each number from 2 to 4, print number, print the number.
