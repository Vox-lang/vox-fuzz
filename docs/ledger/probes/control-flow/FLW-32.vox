(FLW-32 (undocumented precision): `Break.` and `Continue.` work in a
 `Repeat` too, not only in `For each`/`While` - the manual's Loop Control
 section names no construct at all. Break at the first iteration gives one
 `r`; Continue skips only the print for seen=2.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox FLW-32.vox -o p && ./p
 expected output:
   r
   after repeat break
   1
   3
   4
   after repeat continue
)
Repeat 4 times, print "r", If 1 is equal to 1 then, Break. print "tail".
print "after repeat break".
a number called seen is 0.
Repeat 4 times, Increment seen, If seen is equal to 2 then, Continue. print seen.
print "after repeat continue".
