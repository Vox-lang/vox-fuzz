(ARG-31 — using a flag variable before the parse point is a
 compile-time error (LANGUAGE.md:4699). This file has no explicit
 `Parse flags.`, so the parse point is the automatic one after the last
 schema — which makes the read between the two declarations a use before
 parsing, and pins down the "immediately after the LAST flag schema
 declaration" half of ARG-26.
 (Compile-error claim: not assertable from a generated leaf, for the
 same reason as ARG-30.)
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox ARG-31.vox -o p && ./p
 expected output:
   error: Flag variable 'verbose' is used before flags are parsed
   (no binary produced; compile error)
)
a flag called verbose is "-v" or "--verbose", it is a boolean.
Print "verbose is {verbose}".
a flag called output is "-o" or "--output", it is a text with default "auto".
Print "output is {output}".
