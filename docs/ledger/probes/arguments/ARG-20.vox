(ARG-20 — flags may be marked required AND given defaults
 (LANGUAGE.md:4689, "required and/or given defaults"). The combination
 compiles. What it does at runtime is Discrepancy 3: required wins, the
 program aborts before the default can be observed, so the default is
 unreachable for exactly the invocation it exists to serve.
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox ARG-20.vox -o p && ./p
 expected output:
   (exit 1)
)
a flag called output is "-o" or "--output", it is a text with default "out.txt" and is required.
Parse flags.

Print "output is {output}".
