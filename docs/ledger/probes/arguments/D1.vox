(D1 — Discrepancy 1 repro: LANGUAGE.md:4613 defines `arguments's count`
 as "Total number of arguments (including program name)". After
 `Parse flags.` it is not that. Five user arguments are passed; three of
 them are a recognized flag, its value and a second recognized flag;
 `raw` still has all five, and `count` reports 3 — the two survivors
 plus argv[0].
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox D1.vox -o p && ./p -o mine.txt pos1 -v pos2
 Args: -o mine.txt pos1 -v pos2
 expected output:
   arguments actually passed: 5
   arguments's count: 3
)
a flag called verbose is "-v" or "--verbose", it is a boolean.
a flag called output is "-o" or "--output", it is a text.
Parse flags.

a list called original is arguments's raw.
Print "arguments actually passed: {original's length}".
Print "arguments's count: {arguments's count}".
