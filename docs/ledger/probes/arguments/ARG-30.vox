(ARG-30 — declaring a new flag schema after `Parse flags.` is a
 compile-time error (LANGUAGE.md:4497). The diagnostic points at the
 declaration itself; no binary is produced.
 (Compile-error claim: not assertable from a generated leaf, since
 emitting it produces a program that does not compile — outside the
 generator's "legal Vox that should compile and run" contract.)
 Ran: VOX_CORE_PATH=/home/josj/scr/english/vox/coreasm /home/josj/scr/english/vox/target/release/vox ARG-30.vox -o p && ./p
 expected output:
   error: Cannot declare new flags after 'parse flags.'
   (no binary produced; compile error)
)
a flag called verbose is "-v" or "--verbose", it is a boolean.
Parse flags.

a flag called late is "-l" or "--late", it is a boolean.
Print "unreachable".
